# memo 验证记录

## 已执行

- `python -m unittest discover -s tests -v`：14 组通过。
- 搜索：覆盖标题、条目、进度、标签及组合筛选；回收站内容排除，百分号和下划线按普通文字搜索。
- 标题和标签：修改后内容保留、重复标签去重、标签长度和数量验证、清除标签。
- 回收站：移入后隐藏且不可编辑；恢复照片、历史、标签和收藏，释放置顶名额；活动记录不能直接彻底删除。单独条目恢复及彻底删除级联清理已验证。
- 收藏：收藏与取消收藏可保存，不改变置顶状态或内容更新时间；无效状态和不存在的笔记会被拒绝。
- `node --check static/app.js`：语法检查通过。收起展开与收藏筛选的实际浏览器点击仍待人工验收。
- 左侧排序：添加条目、编辑文字、修改状态、添加进度及删除条目后，所属笔记按更新时间前移。
- 置顶：置顶组优先，两组内按更新时间排序；取消置顶恢复普通排序。第 6 个置顶被拒绝，取消一个后可置顶新的；重复置顶同一页不会占用更多名额。
- 可选照片：不上传文件、浏览器提交空文件字段均可创建纯文字笔记，添加条目和进度后可正常删除；无照片时隐藏照片区域。
- 正常路径：上传有效图片、读取照片、添加中文条目、修改内容、切换状态、追加多行进度、删除条目、删除整页。
- 数据完整性：状态变化追加历史，移入回收站保留记录；彻底删除条目或笔记时级联删除其记录，彻底删除整页同时清理照片。
- 持久化及恢复：关闭数据库连接后复制完整数据目录，重新加载应用并指向副本，确认中文条目、进度与照片可读取。此项为应用重载和备份副本恢复测试，不是操作系统重启测试。
- 异常输入：伪造图片、超过 20 MB 的文件、空白内容、过长标题、非法状态、不存在的记录。
- 请求与页面：无操作令牌和跨站提交被拒绝，用户输入中的 HTML 被转义。
- 实际本地服务：`http://127.0.0.1:8000`、CSS 和 JavaScript 均返回 HTTP 200。

测试使用临时数据库与合成图片，不修改用户数据。测试依赖目前会输出一条 httpx 弃用提示，不影响这次测试结果；依赖版本已记录在 requirements.lock.txt。

## 尚未完成的人工验收

当前会话没有可用的内置浏览器，因此没有执行浏览器点击、截图或窄屏视觉验收。HTTP 和模板测试不能替代这些检查。

1. 双击 start.bat，打开首页，确认中文字体、按钮和布局正常。
2. 上传真实手写笔记照片，确认方向和清晰度，点击「查看大图」。
3. 添加一个条目，更新状态，记录两次进展，确认提示和时间正确。
4. 刷新页面，再关闭和重新启动服务，确认数据仍在。
5. 删除时先取消，再确认，检查记录是否符合预期。
6. 缩窄浏览器窗口，确认表单和照片没有横向溢出。
7. 停止服务，复制 data 到备份位置；使用副本恢复并重启，核对照片、条目与历史。

## 验收标准

核心流程全部可完成，提交成功后刷新或重启不丢数据；无效操作不会产生半条记录；完整备份可恢复照片和文字；界面不把失败显示成保存成功。此版本不含模型或 OCR，因此不需要模型准确率评测。
